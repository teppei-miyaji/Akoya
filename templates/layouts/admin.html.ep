<% unless( $controller eq 'admin' && $action_name eq 'index' ){ %>
  <% content_for sidebar => begin %>
    <h3><%= l('label_administration') %></h3>
    <%= include 'admin/menu' %>
  <% end %>
<% } %>

<%= include "layouts/base" %>
